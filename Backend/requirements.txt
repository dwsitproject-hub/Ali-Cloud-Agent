# Cloud Agent Monitoring — Python backend
Flask==3.0.3
APScheduler==3.10.4
python-dotenv==1.0.1

# Alibaba Cloud SDK (RPC-style, product-agnostic via CommonRequest)
aliyun-python-sdk-core==2.16.0

# Database (PostgreSQL via SQLAlchemy + Alembic migrations)
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
psycopg2-binary==2.9.9

# Auth: DWS Hub SSO via OIDC (Authorization Code + PKCE) + session login + CSRF
Flask-Login==0.6.3
Flask-WTF==1.2.1
Authlib==1.3.2          # OIDC client: discovery, PKCE, token exchange, JWKS verify
requests==2.32.3        # HTTP transport for Authlib's Flask integration

# CORS: the standalone frontend calls this API cross-origin with credentials
Flask-Cors==4.0.1

# Production WSGI server
gunicorn==23.0.0

# Tests
pytest==9.0.3
